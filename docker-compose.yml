services:
  php:
    build: .
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html
  mysql:
    image: mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: portfolio
      MYSQL_ROOT_PASSWORD: secret